# Images

Illustration assets go here (see `docs/11_asset_lists.md`). The app runs with
emoji/painted placeholders until these land, so this directory only needs to
exist for the Flutter asset bundler.
