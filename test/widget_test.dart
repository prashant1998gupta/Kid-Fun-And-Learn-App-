// The default Flutter counter test (which referenced a non-existent `MyApp`)
// was removed. Real widget/engine coverage lives in game_progression_test.dart,
// and content coverage in curriculum_test.dart.
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('test suite is wired up', () {
    expect(2 + 2, 4);
  });
}
