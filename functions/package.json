{
  "name": "kidverse-functions",
  "description": "KidVerse Cloud Functions — friends-leaderboard aggregator + weekly reset.",
  "engines": {
    "node": "22"
  },
  "main": "index.js",
  "scripts": {
    "check": "node --check index.js && npm audit --omit=dev --audit-level=high",
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log"
  },
  "dependencies": {
    "firebase-admin": "^13.10.0",
    "firebase-functions": "^7.2.5"
  },
  "private": true
}
